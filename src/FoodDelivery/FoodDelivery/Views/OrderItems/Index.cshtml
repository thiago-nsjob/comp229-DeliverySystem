@model IEnumerable<FoodDelivery.Models.OrderItem>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdRestaurantMenuItem)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Quantity)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PricePerUnity)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OrderNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdRestaurantMenuItem)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Quantity)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PricePerUnity)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.OrderNavigation.IdOrder)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdOrderItem">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdOrderItem">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdOrderItem">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
